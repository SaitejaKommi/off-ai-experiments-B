# Client

Frontend assets for this project.

## Contents

- `extension/` - Browser extension (popup + content script + background worker)

## Run Notes

Load `client/extension/` as an unpacked extension in Chrome/Firefox.
Backend API is expected at `http://localhost:8000`.
